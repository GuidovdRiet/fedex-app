import React, { Component } from 'react';
import { StyleSheet, Text, View, Button, Image } from 'react-native';

class userIsNotHome extends Component {
    render() {
        return(
            <Text>User is not home screen</Text>
        )
    }
}

export default userIsNotHome;