import React, { Component } from 'react';
import { Text } from 'react-native';

class UserProfile extends Component {
    render() {
        return(
            <Text>User profile example</Text>
        )
    }
}

export default UserProfile;